# MyWebsite

This repository contains the static website source in the `frontend/` folder.

Deployment:
- A GitHub Actions workflow `./.github/workflows/deploy.yml` publishes `frontend/` to GitHub Pages (gh-pages branch) on pushes to `main`.
- After pushing, check the repository's Actions tab and Pages settings: https://github.com/shiavm17/MyWebsite/settings/pages
